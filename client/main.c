#include <stdio.h>;
#include <inttypes.h>;
#include <signal.h>;

#include "../serveur/imgdist.h";

int main(int argc, char* argv[]) {
   while (true)
   {
      continue;
   }
   return 0;
}